# cobot3 프로젝트 종합 작업로그

## 1. 작업 목적

Git clone 이후 진행된 AMR, FMS, cuOpt, Nav2, Isaac Sim 연동 작업을 최종
구조 중심으로 축약해 정리한다.

최종 목표는 Isaac Sim의 NodeMap을 FMS가 동기화하고, FMS가 발생시킨
작업을 Active Graph와 cuOpt로 계획한 뒤, 담당 AMR이 계획 경로를
시각화하고 Nav2로 주행하는 것이다.

```text
Isaac Sim NodeMap
    ↓
FMS Active Graph / CSR
    ↓
TaskManager + cuOpt
    ↓
담당 AMR
    ↓
Isaac Sim 경로 시각화
    ↓
Nav2 실제 주행
```

## 2. 초기 AMR·공정 통합 기반

프로젝트 초기에는 Assembly Cell의 작업 요청을 FMS가 수신하고 AMR이
부품 적재와 배송을 수행하는 시연 흐름을 구축했다.

주요 작업:

- AMR End-to-End 시연 구조 구현
- Assembly Shell과 FMS 패키지 충돌 해결
- Assembly Cell–AMR 배송 흐름 통합
- AMR 상태와 작업 완료 이벤트 연동
- 불필요한 Dummy FMS와 이전 Pose Bridge 제거

이 단계에서 AMR 작업 상태, 적재 상태, Pickup/Delivery 흐름의 기본 골격을
확립했다.

## 3. Isaac Sim–AMR ROS 2 Bridge 전환

AMR 제어 방식을 별도 TCP 통신에서 Isaac Sim ROS 2 Bridge 기반으로
전환했다.

주요 작업:

- Isaac Sim에서 AMR odometry 발행
- AMR 위치를 ROS 2 Topic으로 수신
- TCP 기반 AMR 제어 제거
- `/cmd_vel`, odometry, TF 중심의 ROS 2 연결 구조로 통합
- 사용하지 않는 AMR Mission 백업 코드 정리

이후 AMR의 현재 위치는 FMS 작업 요청과 Nav2 주행 판단에 사용하는
기준 데이터가 되었다.

## 4. FMS·cuOpt 작업 스케줄링 기반

FMS가 작업 Queue를 관리하고 AMR이 IDLE일 때 작업을 가져가는 Pull 방식을
구현했다.

```text
AMR IDLE
    ↓ RequestTask Service
FMS Waiting Queue
    ↓ cuOpt
작업 순서 결정
    ↓
선택 Task를 AMR에 할당
```

주요 작업:

- FMS Waiting/Active Task 관리
- AMR 상태 저장 및 완료 Task 정리
- `/fms/request_task` Service 구축
- cuOpt cost matrix와 작업 순서 계산
- FMS–cuOpt 데이터 흐름 정리

초기 cuOpt 구현은 Scenario 0 시연을 위해 Parts Supermarket과
`cell_a/b/c`의 고정 위치를 사용했다. 이 구조는 이후 실제 NodeMap 기반
Task 구조로 교체했다.

## 5. Nav2 주행 연동

AMR이 FMS에서 받은 Pickup/Delivery 좌표를 Nav2
`NavigateToPose` Action으로 전달하도록 변경했다.

주요 작업:

- AMR Node에 `/navigate_to_pose` Action Client 추가
- 직접 Goal 전달 방식 제거
- Pickup 도착, 적재, Delivery 도착 상태 전이 구현
- Nav2 Goal 결과와 timeout에 따른 실패 처리
- AMR Digital Twin과 Nav2 통신 확인

AMR의 기본 작업 상태는 다음 순서로 구성된다.

```text
IDLE
→ TASK_ASSIGNED
→ MOVING_TO_PICKUP
→ ARRIVED_PICKUP
→ LOADING
→ LOAD_COMPLETE
→ MOVING_TO_DELIVERY
→ ARRIVED_DELIVERY
→ DELIVERY_COMPLETE
→ MISSION_COMPLETE
→ IDLE
```

실패 시 `ERROR / TASK_FAILED` 상태로 전환한다.

## 6. cuOpt 실행환경 정리

cuDF, Numba, coverage 사이의 버전 충돌을 정리하고 프로젝트 venv에 cuOpt
실행환경을 구성했다.

확인사항:

- cuOpt와 cuDF는 프로젝트 `.venv`에서 사용
- coverage 7.4.4와 Numba API 충돌 처리
- FMS 실행 시 시스템 Python과 venv Python이 섞이지 않아야 함

현재 `ros2 run`으로 생성된 FMS 엔트리포인트는 `/usr/bin/python3`을 사용할
수 있으므로 cuDF가 설치된 venv Python으로 실행되도록 빌드·실행 방식을
정리할 필요가 있다.

## 7. 1차 작업 — Isaac Sim NodeMap 동기화

Isaac Sim Stage의 NodeMap을 FMS가 직접 사용할 수 있도록
`ExtNodeMapBuild` Extension과 ROS 2 인터페이스를 구축했다.

### Isaac Sim Extension

```text
simulation/isaac_sim/ExtNodeMapBuild/
```

Extension은 현재 Stage의 다음 Prim을 읽는다.

```text
/World/WaypointGraph/Nodes
/World/WaypointGraph/Edges
```

추출 데이터:

- Node ID, 이름, 타입, XYZ 좌표
- Edge 시작/도착 Node, weight, bidirectional

Custom Attribute가 없으면 `Node_7`, `Edge_7_9`와 같은 Prim 이름을
파싱하여 Node ID와 연결 정보를 복원한다.

### NodeMap 통신

- `/get_node_map`: FMS가 전체 NodeMap을 요청하는 Service
- `/node_map_changed`: Stage 또는 NodeMap 변경을 알리는 Event Topic
- revision: Event와 Service 응답의 NodeMap 버전 검증

```text
Isaac Sim NodeMap 변경
    ↓ /node_map_changed
FMS revision 확인
    ↓ /get_node_map
최신 NodeMap 수신 및 교체
```

Isaac Sim GUI가 정지하지 않도록 Extension은 메인 update event에서
`rclpy.spin_once(..., timeout_sec=0.0)`으로 ROS callback을 처리한다.

## 8. FMS Active Graph와 CSR

수신한 Node와 Edge는 `NodeMapGraphManager`가 관리한다.

관리 데이터:

- 전체 Node/Edge
- Node/Edge `available`
- 활성 Edge와 neighbors
- Active CSR
- NodeMap revision

CSR 구성:

```text
node_ids : 실제 Isaac Sim Node ID 목록
offsets  : Node별 인접 정보 시작 위치
indices  : node_ids 배열 기준 압축 인덱스
weights  : Edge 이동비용
```

unavailable Node나 Edge는 Active Graph와 CSR에서 제외한다. 외부에 전달하는
경로는 CSR 압축 인덱스가 아니라 실제 Node ID 배열이다.

AMR이 Track 밖에 있는 경우를 위해 현재 AMR 좌표에서 가장 가까운 활성
Node를 복귀 Node로 선택한다. 현재는 장애물이 없는 개방 공간을 전제로
XY 직선거리를 사용한다.

관련 커밋:

```text
628406f feat: Isaac Sim과 FMS 간 NodeMap 이벤트 동기화 추가
```

## 9. 2차 작업 — NodeMap Task와 cuOpt 경로계획

Scenario 0의 고정 위치와 사전 정의 Task를 제거하고 `TaskManager`가
NodeMap의 시작 Node와 도착 Node로 작업을 생성하도록 변경했다.

```text
Task
├── task_id
├── start Node
├── goal Node
├── status
└── planned route
```

`TaskManager`는 Waiting Queue, Active Task, 작업 할당과 완료 상태를
관리한다.

cuOpt 비용은 Active CSR의 최단 이동비용으로 구성한다.

```text
최초 작업
AMR 복귀 Node → Task 시작 Node → Task 도착 Node

연속 작업
이전 Task 도착 Node → 다음 Task 시작 Node → 다음 Task 도착 Node

종료
마지막 Task 도착 Node → AMR 복귀 Node
```

cuOpt는 Waiting Task의 수행 순서를 결정하며, 각 Task 내부의 실제 Node
경로는 같은 Active CSR의 최단경로 결과를 사용한다.

삭제한 Scenario 0 코드:

- Parts Supermarket 고정 위치
- `cell_a/b/c` 목적지
- 고정 Task 데이터
- `defined.py`

## 10. FMS–AMR Planned Route 전달

`RequestTask.srv` 응답에 다음 경로 데이터를 추가했다.

- `node_map_revision`
- `route_node_ids`
- `route_x`, `route_y`, `route_z`
- `route_total_cost`

AMR은 IDLE 상태이고 odometry를 받은 경우 `/fms/request_task`를 호출한다.
FMS는 cuOpt가 선택한 Task와 planned route를 응답하고 Task를 ASSIGNED로
변경한다.

## 11. AMR–Isaac Sim 경로 시각화

경로 전달 주체는 FMS가 아니라 실제 작업 담당 AMR로 확정했다.

```text
FMS
    ↓ RequestTask Response
담당 AMR
    ↓ /visualize_route Action
Isaac Sim Extension
    ↓ Result success
담당 AMR
    ↓ Nav2 주행
```

추가 인터페이스:

```text
interfaces/action/VisualizeRoute.action
```

Extension 검증 항목:

- NodeMap revision 일치
- 모든 Node ID 존재
- 연속 Node 사이 Edge 존재
- Node ID와 XYZ 배열 길이 일치

AMR은 시각화 Action의 최종 Result가 성공한 경우에만 Nav2 주행을
시작한다. 실패하면 `TASK_FAILED`로 처리한다.

Isaac Sim PC가 메인 ROS 2 Workspace 없이 실행될 수 있도록 Extension
내부에 독립 `ros2_interfaces` 패키지와 실행 스크립트를 추가했다.

```text
simulation/isaac_sim/ExtNodeMapBuild/ros2_interfaces/
simulation/isaac_sim/run_isaac_sim.sh
```

## 12. Multi-AMR 적용 상태

Isaac Sim의 AMR 통신은 namespace로 구분된다.

```text
/AMR1/odom
/AMR1/cmd_vel
/AMR1/scan
/AMR1/tf
/AMR1/tf_static
```

AMR1 검증에서는 기존 `/amr/odom` 구독을 다음과 같이 remap했다.

```bash
ros2 run amr_control amr_node \
  --ros-args -r /amr/odom:=/AMR1/odom
```

현재 `amr_id=AMR_01`과 일부 ROS 이름은 코드에 고정되어 있다. 실제
Multi-AMR 운용 전 parameter 또는 namespace 기반으로 분리해야 한다.

## 13. 최종 통합 검증 결과

실제 Isaac Sim NodeMap으로 다음 흐름을 검증했다.

```text
NodeMap revision : 1
Node count       : 14
Edge count       : 16
Task             : Node_8 → Node_6
Planned route    : 8 → 9 → 7 → 6
Route cost       : 50.354
```

확인 완료:

- Isaac Sim → FMS NodeMap Service
- NodeMap 변경 Event와 revision 동기화
- Active CSR 구성
- TaskManager 검증 Task 생성
- CSR 비용 기반 cuOpt 실행
- FMS → AMR Task와 planned route 전달
- AMR → Isaac Sim VisualizeRoute Action
- Extension 경로 검증 및 시각화 완료 응답

실제 주행은 검증 당시 `/navigate_to_pose` Action Server가 없어 시작점 이동
단계에서 중단됐다.

## 14. 현재 남은 작업

1. Nova Carter를 Node Track의 유효한 위치에 배치
2. AMR1 Nav2 Server와 cmd_vel, odometry, TF, scan 연결
3. planned route의 중간 Node를 Nav2 주행에 반영
4. 현재 위치에서 작업 시작 Node로 복귀하는 경로 처리
5. 계획 경로와 실제 주행 궤적 비교
6. Task 두 건 이상으로 cuOpt 작업 순서 최적화 검증
7. unavailable Node/Edge 변경 시 재계획 검증
8. 벽과 장애물을 고려한 복귀 Node 선택 보강
9. Multi-AMR ID와 ROS namespace 분리
10. FMS venv Python 실행 방식 정리

## 15. 주요 커밋

```text
e3fb6ed feat: implement AMR end-to-end demo
7272e05 feat: integrate assembly cells with AMR delivery flow
cee4bb5 feat: publish AMR odometry via Isaac ROS2 bridge
2caf11c refactor: replace AMR TCP control with ROS2 bridge
c367ae7 feat: integrate pull-based FMS and cuOpt scheduling
c63cb81 feat: integrate Nav2 navigation with AMR digital twin
c831af5 feat: integrate Nav2 with AMR control and FMS
e3f1e1e 가상환경 : coverage 7.4.4와 Numba API 충돌 처리
628406f feat: Isaac Sim과 FMS 간 NodeMap 이벤트 동기화 추가
640a08a cuOpt 검증작업 전 복귀점
a4eaf30 feat: NodeMap CSR 기반 cuOpt 경로계획 및 AMR 시각화 연동
```

현재 작업 브랜치:

```text
feature/FMS_syncNodeMap
```
