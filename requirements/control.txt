# cobot3_project — FMS + cuOpt runtime dependencies
#
# Runtime:
#   Ubuntu 24.04
#   Python 3.12
#   ROS 2 Jazzy (provided by /opt/ros/jazzy)
#   NVIDIA CUDA 13.x environment
#
# ROS 2 packages such as rclpy and geometry_msgs are provided by
# the system ROS 2 Jazzy installation through PYTHONPATH.
#
# General Python dependencies required by ROS 2 packages are
# installed explicitly in this virtual environment.

--extra-index-url https://pypi.nvidia.com

# ROS 2 Python compatibility dependencies
Jinja2==3.1.2
typeguard==4.1.5
setuptools>=68.1.2,<80
coverage>=7.6,<8

# NVIDIA cuOpt
cuopt-cu13==26.8.0
